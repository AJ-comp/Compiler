<UserControl x:Class="WpfApp.Views.ErrorList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodel="clr-namespace:WpfApp.ViewModels"
             xmlns:local="clr-namespace:WpfApp.Views"
             xmlns:lex="http://wpflocalizeextension.codeplex.com" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="600">
    <Grid>
        <ListView>
            <ListView.Resources>

            </ListView.Resources>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header=" "/>
                    <GridViewColumn Header="{lex:Loc Key=ErrorCode}"/>
                    <GridViewColumn Header="{lex:Loc Key=ErrorExplanation}"/>
                    <GridViewColumn Header="{lex:Loc Key=ErrorProject}"/>
                    <GridViewColumn Header="{lex:Loc Key=ErrorFile}"/>
                    <GridViewColumn Header="{lex:Loc Key=ErrorLine}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
