// The AJ language is nested under Parse.FrontEnd.AJ, so before the engine was
// renamed it implicitly saw the engine's ancestor namespaces (Parse, Parse.FrontEnd).
// The engine root is now Janglim, so those are imported explicitly here.
global using Janglim;
global using Janglim.FrontEnd;
